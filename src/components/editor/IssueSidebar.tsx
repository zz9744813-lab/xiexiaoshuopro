'use client'
import React from 'react'

export default function IssueSidebar({ chapterId, projectId }: { chapterId: string; projectId: string }) {
  return <aside className="issue-sidebar">Issue Sidebar</aside>
}
