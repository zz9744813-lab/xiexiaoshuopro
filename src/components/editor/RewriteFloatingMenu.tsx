'use client'
import React from 'react'

export default function RewriteFloatingMenu({ selectedText, onRewrite }: { selectedText: string; onRewrite: (text: string) => void }) {
  return <div className="rewrite-menu">Rewrite Menu</div>
}
