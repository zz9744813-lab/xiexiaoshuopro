# Mastra Agent 运行时配置
# Agent 级别的参数在此配置，运行时由 simulation workflow 读取

agent:
  # 默认最大推演轮数。开放式探索场景建议 150+
  # 注意：此值仅作参考默认值，运行时 API 可以覆盖
  max_turns: 200

  # 旁白频率（每 N 轮插入一次旁白）
  narrator_interval: 3

  # Director 决策温度
  temperature: 0.5

  # Character 发言温度
  character_temperature: 0.9
